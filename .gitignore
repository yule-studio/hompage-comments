# secrets — the GitHub token lives here and must never be committed
.env
*.env
!.env.example

# gradle / build
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# ide
.idea/
*.iml
.vscode/

# os
.DS_Store

# resume-api: download log (other people's names and emails) and any local
# copy of the source résumé. Neither belongs in the repo.
data/
*.db
*.pdf
